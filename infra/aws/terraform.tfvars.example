# Copy this file to terraform.tfvars and fill in your values
# cp terraform.tfvars.example terraform.tfvars

project_name = "ragdemo"
region       = "ap-southeast-2"
environment  = "dev"

openai_api_key = "sk-..."   # replace with your key

# GitHub repo for OIDC (format: org/repo — e.g. "jinyoungkim/RAG-azure")
github_repo = "your-github-org/RAG-azure"

# OpenSearch is hosted on the EC2 instance via Docker Compose
# (managed Amazon OpenSearch Service is not used in this stack).

# ecs_cpu    = 512
# ecs_memory = 1024

# container_image = "123456789.dkr.ecr.ap-southeast-2.amazonaws.com/ragdemo-dev-backend:latest"

# allowed_tenant_ids = "tenant-aws-1,tenant-aws-2"
